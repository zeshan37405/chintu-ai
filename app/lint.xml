<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!-- Android 36 is the newest stable SDK installed by the release runner. -->
    <issue id="OldTargetApi" severity="ignore" />
    <issue id="GradleDependency" severity="ignore" />

    <!-- The UI is intentionally generated in Java and Urdu-first. -->
    <issue id="SetTextI18n">
        <ignore path="src/main/java/com/zeshan/chintuai/ChintuActivity.java" />
    </issue>
    <issue id="RtlHardcoded">
        <ignore path="src/main/java/com/zeshan/chintuai/ChintuActivity.java" />
    </issue>

    <!-- Platform registerReceiver flags only exist on API 33; the pre-33 branch is required. -->
    <issue id="UnspecifiedRegisterReceiverFlag">
        <ignore path="src/main/java/com/zeshan/chintuai/ChintuActivity.java" />
    </issue>

    <!-- minSdk is 26; this explicit branch preserves readable foreground-service startup code. -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/java/com/zeshan/chintuai/ChintuActivity.java" />
    </issue>

    <!-- RECORD_AUDIO is checked before the foreground service starts. The builder lives in a
         private API-33-only method, so lint cannot follow the permission and SDK guards. -->
    <issue id="MissingPermission">
        <ignore path="src/main/java/com/zeshan/chintuai/HandsFreeVoiceService.java" />
    </issue>
    <issue id="UseRequiresApi">
        <ignore path="src/main/java/com/zeshan/chintuai/HandsFreeVoiceService.java" />
    </issue>
</lint>
